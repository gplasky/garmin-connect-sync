# HW5 requirements
#  At least one function
#  At least one if statement (can be if/else or if/elif/else)
#  At least one list OR one dictionary
#  Some level of communication or interaction with a user (printed status updates on code as it runs, requests for inputs, or reading in data from the user’s (your) computer)
#  At least one library

class GarimDataPump():

    def __init__(self):
        return

    def sync_data(self):
        return


if __name__ == "__main__":
    GarimDataPump.sync_data()
